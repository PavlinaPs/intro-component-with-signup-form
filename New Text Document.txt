    errors.forEach(error => {
         if(error.previousElementSibling.value === null) {
            console.log(error.previousElementSibling.value);
            return;
        } else {
            e.preventDefault();
            console.log(error);
            displayErrorMessage(error);
            return;
        }
    })

    // if (!emailValidationRegex.test(enteredEmail.value)) {
    //     displayErrorMessage(enteredEmail);
    // }

e.preventDefault()